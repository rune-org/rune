# Application Settings
ENVIRONMENT=prod
APP_NAME="Rune API"
#comma separated list of allowed origins or single origin without a comma
CORS_ORIGINS=http://localhost:3000,http://frontend:3000,http://127.0.0.1:3000 


# Database Settings
POSTGRES_USER=rune
POSTGRES_PASSWORD=rune_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=rune_db

# Optional: Full database URL (overrides individual settings above)
# DATABASE_URL=postgresql+asyncpg://rune:rune_password@localhost:5432/rune_db


# JWT Settings
JWT_SECRET_KEY=gdfshdshdhsdghrbrhhjtjjrnnrrwh
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=2
REFRESH_TOKEN_EXPIRE_DAYS=30


# Redis Settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your-redis-password-here

# RabbitMQ Settings
RABBITMQ_WORKFLOW_QUEUE=workflow.execution
RABBITMQ_TOKEN_QUEUE=execution.token
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=rune
RABBITMQ_PASSWORD=rune_password

# Optional: Full RabbitMQ URL (overrides individual settings above)
RABBITMQ_URL=amqp://rune:rune_password@localhost:5672/

ENCRYPTION_KEY=8eLl-T7Un3Qj5vN9sP2wK4bR6xZ0cF1mA8yD7gH3iE0=

#GOOGLE API KEY
GOOGLE_API_KEY=your-google-api-key-here

# Smith AI Settings
SMITH_MODEL=gemini/gemini-2.5-flash-lite
SMITH_TEMPERATURE=0.3

# ---------------------------------------------------------------------------
# SAML SSO
# ---------------------------------------------------------------------------
# SAML_SP_BASE_URL  — the public URL the BROWSER uses to reach this API.
#                     Used to build ACS, metadata, and SLO URLs that are
#                     registered with the IdP.  Must NOT have a trailing slash.
#
#   Full-stack (nginx)  → https://localhost/api   ← default in config.py
#   Local dev (no nginx)→ http://localhost:8000   ← set this in your .env
#
# Why no /api in the dev value?
#   nginx rewrites /api/* → / before forwarding to the container, but in dev
#   you hit the FastAPI dev server directly on port 8000 — no rewriting, no
#   /api prefix in the actual route path.
#
# SAML uses browser-mediated POST binding for ACS, so even though Authentik
# runs inside Docker, it is NOT the one POSTing back — the browser is.
# http://localhost:8000 is perfectly reachable by the browser.
#
# NOTE: Changing SAML_SP_BASE_URL after an IdP config has been created means
# the SP entity ID changes, which breaks the existing IdP trust.  You must
# delete the RUNE SAML config and re-register the SP in Authentik.
SAML_SP_BASE_URL=https://localhost/api

# SAML_FRONTEND_URL — where the API redirects after a successful (or failed)
#                     SSO assertion.  Usually just the root of your frontend.
#   Full-stack (nginx)  → https://localhost
#   Local dev (no nginx)→ http://localhost:3000
SAML_FRONTEND_URL=https://localhost

# How long (seconds) a processed SAML assertion ID is remembered in Redis
# for replay-attack protection.  Default (300 s) is fine for most setups.
# SAML_ASSERTION_ID_TTL=300