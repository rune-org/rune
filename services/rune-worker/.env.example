# RabbitMQ Configuration
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Workflow Queue Configuration
WORKFLOW_QUEUE_NAME=workflow.execute
WORKFLOW_PREFETCH=10
WORKFLOW_CONCURRENCY=1

# -----------------------------------------
# Additional Queue Names (Info Only)
# -----------------------------------------
# These queues are used by the worker for publishing messages:
# - workflow.execution: NodeExecutionMessage (next nodes to execute)
# - workflow.node.status: NodeStatusMessage (node status updates)
# - workflow.completion: CompletionMessage (workflow completion events)