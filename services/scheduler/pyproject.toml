[project]
name = "scheduler"
version = "0.1.0"
description = "Standalone polling scheduler for Rune workflow automation"
requires-python = ">=3.13,<3.14"
dependencies = [
    "asyncpg==0.30.0",
    "httpx>=0.28.0",
]
