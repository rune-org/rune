# ⚠️ GENERATED FILE - DO NOT EDIT MANUALLY
# This file is generated from dsl/dsl-definition.json
# To update, modify dsl/dsl-definition.json and run: make dsl-generate
#
# This enum should be kept in sync with credential types defined in dsl-definition.json
# Current credential types in DSL:
{% for cred_type, cred_def in dsl.credential_types.items() %}
# - {{ cred_type }}: {{ cred_def.description }}
{% endfor %}

from enum import Enum


class CredentialType(str, Enum):
    """Credential type enumeration.
    
    NOTE: This enum should match the credential types in dsl/dsl-definition.json.
    If you add a new credential type to the DSL, update this enum accordingly.
    """

{% for cred_type, cred_def in dsl.credential_types.items() %}
    {{ cred_type.upper() }} = "{{ cred_type }}"
{% endfor %}
    # Additional types not in DSL (for backward compatibility or special cases)
    API_KEY = "api_key"
    OAUTH2 = "oauth2"
    BASIC_AUTH = "basic_auth"
    TOKEN = "token"
    CUSTOM = "custom"

